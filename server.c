

#include <stdio.h>
int main( int argc, const char* argv[] )
{
    char floppy[] = argv[1];
	  printf( "\nFloppy %s opened...\n",floppy);
	  //fmount
	  
	  //loop waiting for input
	  while(0)
	  {
	      //if input is recieved, return the designated sector info in UDP
	  
	  }
	  
	  //fumount
	  return 0;
}
